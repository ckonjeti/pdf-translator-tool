# Only ignore what we absolutely don't need
node_modules/
client/node_modules/
.git/
*.log
.env.local
.env.development.local
.env.test.local
.env.production.local

# Ignore Tesseract language files (no longer needed with GPT-4 Vision)
*.traineddata

# Keep everything else including client/public/index.html